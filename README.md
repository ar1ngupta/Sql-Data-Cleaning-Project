# Sql-Data-Cleaning-Project
Cleaned and transformed raw data using SQL by removing duplicates, handling null values, correcting inconsistencies, and standardizing formats. Applied SQL queries like UPDATE, DELETE, CASE, and JOIN to enhance data quality, enabling accurate analysis and reporting.
I had taken layoff data of a company in which Executed a comprehensive data cleaning project using SQL to transform a raw, inconsistent dataset into a reliable and analysis-ready format. Applied key data preprocessing techniques such as removing duplicates, handling null values, standardizing inconsistent data formats, correcting misspelled entries, and filtering irrelevant records. Utilized SQL commands like DELETE, UPDATE, CASE, TRIM, JOIN, and PARTITION BY to ensure data integrity and consistency. The cleaned dataset significantly enhanced the accuracy of downstream analysis and reporting, leading to better business insights.
